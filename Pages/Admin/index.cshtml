@page
@model CotizacionesPersonales.Pages.IndexModel

@{
    ViewData["Title"] = "Admin Home";
}

<h3>DashBoard</h3>

<table>
    <caption>Clientes</caption>
    <thead>
        <tr>
            <th>Nombre </th>
            <th>telefono </th>
            <th>Direccion </th>
            <th>Email </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var cliente in Model.Clientes ){
            <tr>
                <td>@cliente.NombreCliente</td>
                <td>@cliente.TelefonoCliente</td>
                <td>@cliente.DireccionCliente</td>
                <td>@cliente.EmailCliente</td>
            </tr>
        }
    </tbody>
</table>
<br />

@*
public string NombreServicio { get; set; }
        public string DescripcionServicio { get; set; }
        public decimal PrecioServicio { get; set; }
*@

<table>
    <caption>Servicios</caption>
    <thead>
        <tr>
            <th>Servicio</th>
            <th>Descripcion </th>
            <th>Precio </th>
            <th>Detalle </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var servicio in Model.Servicios ){
            <tr>
                <td>@servicio.NombreServicio </td>
                <td>@servicio.DescripcionServicio </td>
                <td>@servicio.PrecioServicio.ToString("C", CultureInfo.CreateSpecificCulture("en-US")) </td>
                <td> <a asp-page="/Admin/ViewDetalleServicio" asp-route-id ="@servicio.ServicioID">Ver</a>  </td>
            </tr>
        }
    </tbody>
</table>

